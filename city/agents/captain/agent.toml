# Captain agent — pinned to claude-opus-4-8 (crew model policy, RULINGS #9).
# The "claude-opus" provider (city.toml) inherits the builtin "claude" preset
# and appends the exact model id via args_append.
provider = "claude-opus"
