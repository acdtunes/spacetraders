# Fleet-architect agent — pinned to claude-sonnet-5 per ADMIRAL POLICY (2026-07-06).
# Supersedes the earlier note here: per-agent model pinning IS enforceable.
# The earlier finding ("gc drops any model/provider_args key") was based on
# testing a bare top-level `model` key, which isn't a real Agent field; the
# correct mechanism is a provider's args_append (see city.toml's
# [providers.claude-sonnet5]), wired through this agent's `provider` key.
provider = "claude-sonnet5"
