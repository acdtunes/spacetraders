- friction: (s54) The Admiral's goals-level message RE-APPEARED in the session prompt despite being fully answered the prior session (s53/d-60, Horizon plan written into strategy.md). A message flagged "clears automatically" that re-injects forces a full re-triage just to confirm nothing changed — a re-handling tax. A dedup on message id, or surfacing "already-answered (see d-60)" alongside it, would let the Captain reaffirm in one line instead of re-deriving.
- friction: (s54) Still no completion EVENT surfaced by the coordinator — per-contract picks and provider distance are reconstructed by hand from `container logs <coordinator>`, and cycle NET is hand-paired from ledger rows (no `contract list` / P&L verb). Recurring observability tax on every heartbeat.
- friction: (s54) `ledger list` still rejects bare / `--agent` invocation and demands `--player-id` — inconsistent with the rest of the CLI that accepts `--agent`.
- friction: (s55) The L28 desynced-Balance false alarm RECURRED a 3rd time (s39/s51/s55): one corrupt PURCHASE_CARGO Balance row (-18,197) fired FOUR spurious credits.threshold DOWN events (100k/250k/500k/1M) + a negative 24h delta (-193,197) in the fleet report, forcing a full hand re-derivation of true treasury (~1,813,889) before any action. Candidate fix (queued since s51): reconcile the ledger Balance column, or compute credits.threshold off the last CONTRACT_* anchor instead of the raw row, so a single desynced row can't manufacture a fleet-wide treasury panic.
- friction: (s58) `operations start --manufacturing` (and `goods produce`) exposes NO ship-exclusion/reservation flag — it auto-discovers idle haulers, unlike gas extraction which takes explicit `--siphons`/`--storage`. With a single productive hauler that is also the contract earner, this makes a clean *parallel* manufacturing experiment impossible: the engine will race the contract coordinator (L46c) for the same ship, and I can't fence it off. Candidate fix: a `--ships`/`--exclude-ships` flag (or a fleet-level ship-reservation) for manufacturing so a stream can be pinned to a dedicated hauler without stealing the contract earner. This is the actual gate on validating manufacturing $/h without corrupting the contract loop.
