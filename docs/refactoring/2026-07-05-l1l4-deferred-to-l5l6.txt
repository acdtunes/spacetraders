SKIP: cargo_transaction.go executeTransactions: trailing dead comment 'Update running balance for next batch (approximate, without initial balance)' with no following code — safe L1 comment removal but outside the region I edited; left for a focused pass
SKIP: jump_ship.go JumpShipHandler.Handle (~140 lines, numbered-step comments): L2 extract-function candidate (navigateToJumpGateIfNeeded / executeJump). Skipped as too risky for a mechanical pass — heavy shared-local reassignment (ship, navigatedToGate, jumpGateSymbol, currentSystem) would require multi-value returns; behavior-preservation risk outweighs value. Defer to L5-L6.
SKIP: cargo_transaction.go getTransactionLimit recomputes waypointSymbol already computed in Handle — trivial duplication, not worth a cross-method change
SKIP: strategies package: single-use string literals ('conservative','minimal','purchase','sell') not extracted to constants — each used once, no repetition to deduplicate
SKIP: No L3/L4 undertaken: files are already cohesive, functions single-abstraction, no 4+ param primitive-obsession clusters beyond existing well-named parameter structs; most packages have no _test.go (rule 6 conservatism)
SKIP: client.go request()/requestWithErrorParsing: the 429/503/5xx retry+backoff branches share structure but differ subtly (Retry-After header handling, DEBUG prints, finalStatusCode assignment). Extracting a common retry helper risks altering ordering/metrics/error semantics — deferred to L5-L6.
SKIP: client.go: ~6 near-identical cargo-inventory conversion blocks (GetShip, ListShips, ExtractResources, SiphonResources, TransferCargo, SupplyConstruction) each iterate a DISTINCT anonymous struct type into shared.CargoItem; a shared helper requires Go generics or a shared named DTO — deferred.
SKIP: client.go: removing the DEBUG fmt.Printf lines would clean the retry loop but changes stdout side-effects (behavior) — deferred.
SKIP: metrics package (manufacturing/market/api/etc.): repeated prometheus GaugeVec/CounterVec construction and Register() slice loops are declarative metric definitions; deduping via table-driven construction would obscure per-metric config and add indirection with no behavior benefit — intentionally left.
SKIP: graph_builder.go/BuildSystemGraph: single 170-line function with pagination + graph/edge building could be split, but the package's only test is client_metrics_test.go and the function's internal helpers touch shared graph state; extraction deferred as higher-risk L3.
SKIP: health_monitor.go: remove dead empty conditional (routes[shipSymbol].Status()==RouteStatusCompleted branch) — no-test package, kept strictly behavior-safe
SKIP: Extract shared enum-like string literals (DB 'RUNNING'/'IN_TRANSIT', market 'WEAK'/'STRONG'/'ABUNDANT' etc.) to constants — these cross package/API boundaries; unsafe without wider-than-partition scope
SKIP: manufacturing_opportunity.go calculateScore weights/thresholds (0.40/0.30/500.0/20.0/+100) — heavily documented; extraction risked reducing clarity, no tests to protect
SKIP: container.go const block misalignment (ContainerType values lines ~54-58) — file was not otherwise edited, so left per gofmt-only-on-touched rule
SKIP: ledger/transaction.go: remove redundant exported GetCategory() — exported symbol with unverified external callers, not safe to delete
SKIP: fixer.go ProcessOne long pipeline function — extracting stages would be an L2 win but each stage mutates report status with subtle early-return semantics; judged too risky for behavior preservation despite test coverage
SKIP: ContainerRunner.GetLogs (exported, zero callers repo-wide) left in place — deleting exported API judged out of scope for L1; candidate for L5-L6 dead-API pruning
SKIP: daemon_service_impl.go: many RPC methods have 4+ params and repeated playerID(*int) deref + resolvePlayerID boilerplate; introducing a request-context/parameter object is an L4 transform that would ripple into cmd/ and other adapters outside the partition
SKIP: command_factory_registry.go: config-key string literals (e.g. 'ship_symbol','container_id','system_symbol') repeated across factories and error messages; extracting to named constants is L1 but high-churn and coupled to error text — deferred
SKIP: daemon_server.go RecoverRunningContainers is long with nested worker-skip branches (guard-clause/extract candidate at L2/L3) but is delicate recovery/concurrency code — left untouched for safety
SKIP: cmd/spacetraders-daemon/main.go: run() is a ~500-line god function (L2/L3 extraction candidate) but has no test coverage and shares dozens of local variables across ~40 handler registrations; extraction risks behavior change without a safety net
SKIP: cmd/spacetraders-daemon/main.go: the ~40x repeated 'if err := mediator.RegisterHandler[*T](med, h); err != nil { return fmt.Errorf("failed to register X handler", ...) }' pattern (L2 dedup) needs a generic helper whose handler param type varies per call; too risky without tests
SKIP: internal/infrastructure/config/defaults.go: many single-use default literals (ports, timeouts, sizes) could be named constants (L1), but each is already contextualized by its field name and extraction adds ~50 symbols of churn with low readability gain; repeated 'localhost'/'spacetraders' literals are semantically distinct so a shared constant would wrongly couple them
SKIP: internal/infrastructure/pidfile/pidfile.go: Acquire and KillExisting share a read+TrimSpace+Atoi snippet (L2 dedup candidate) but their surrounding error/control flow diverges; no test coverage, so left untouched to preserve behavior
SKIP: internal/adapters/graph/graph_service.go: GetGraph/GetWaypoint have 4 params (L4 parameter-object candidate) but implement cross-partition interfaces ISystemGraphProvider/IWaypointProvider; signature change would break external callers
SKIP: ledger/queries/get_transactions.go: resolvePlayerID is a needless single-line wrapper around shared.NewPlayerID (L4 inline candidate) — left conservative in a no-test package
SKIP: scouting/commands (scout_markets.go, scout_tour.go, refresh_market_data.go): extract repeated log-level literals 'INFO'/'WARNING'/'ERROR'/'DEBUG' and 'scout_all_markets_reset' into constants (L1) — high churn across no-test files, skipped for a later pass
SKIP: assign_scouting_fleet.go: extract 'MARKETPLACE'/'FUEL_STATION' waypoint-trait/type literals into named constants (L1) — skipped conservatively (no tests)
SKIP: record_transaction.go: OrderBy magic string 'timestamp DESC, created_at DESC, id DESC' and 'UNKNOWN' default agent could become constants — left untouched; file carries meaningful domain comments and has tests, no compelling L1/L2 win
SKIP: setup/handler_registry.go and player commands: functions with 4+ params (buildFinalResponse, register handlers) are candidates for parameter objects (L4) — skipped as high-churn with low value and behavior-neutral risk in no-test packages
SKIP: Split god file daemon_client.go (1162 LOC) into cohesive same-package files (ship/container/goods/construction/gas ops) - compiler-verifiable move but high churn; deferred due to unstable concurrent-build environment making verification unreliable
SKIP: Introduce parameter objects for daemon_client.go methods with 4+ params (NavigateShip, JumpShip, JettisonCargo, ScoutTour, ScoutMarkets, PurchaseShip, BatchPurchaseShips, StartParallelManufacturingCoordinator, GasExtractionOperation, StartConstructionPipeline) - exported methods requiring repo-wide call-site verification; deferred (L4)
SKIP: Introduce a player-identity value object for the recurring (playerID int, agentSymbol string) cluster across daemon_client methods - exported API change; deferred (L4)
SKIP: Deduplicate the repeated config.LoadConfig + database.NewConnection + repository wiring block shared by ship.go sell command and player.go - cross-file setup dedup with differing wiring; deferred (L2/L3)
SKIP: Delete redundant inline noise comments across command files and daemon_client.go (e.g. // Build request, // Call gRPC service, // Get daemon client, // Convert to client response type) - left to avoid broad subjective churn; deferred (L1)
SKIP: ship.go line 53 commented-out newShipJettisonCommand with TODO marker - intentional, left untouched
SKIP: ENVIRONMENT: final `go build ./...` and `go test` are red ONLY due to concurrent edits by other agents in shared packages OUTSIDE my partition (internal/domain/navigation removed Ship.Cargo(); also breakages seen in internal/domain/goods, internal/domain/shared, internal/application/manufacturing). My partition passed vet+tests green at each L1/L2/L3 checkpoint before this cascade; rule 4 forbids me touching those files.
SKIP: L4 parameter objects: delivery_executor.go ProcessSingleDelivery/ExecutePurchaseLoop/executeSinglePurchaseTrip (8 params each) and contract/ship_selector.go SelectClosestShip (8 params) — deferred; exported signatures with call sites and a tested service, params are distinct (not a repeated data clump), behavior-risk outweighs value
SKIP: L2/L3 dedup of near-duplicate handlers accept_contract.go vs fulfill_contract.go (loadContract/saveContract/fetchCurrentCredits/callXContractAPI/recordContractX) — high churn with subtle behavioral differences (fulfill validates player ownership; different transaction types/messages); deferred to L5-L6
SKIP: L4 Middle Man: FleetPoolManager now only wraps mediator via GetMediator() — candidate for inline/removal into the coordinator, but that is a larger structural change left for a later pass
SKIP: gofmt: 6 pre-existing unformatted files in the partition (balance_ship_position.go, negotiate_contract.go, contract_lifecycle_service.go, ship_pool_manager.go, siphon_resources.go, cargo_manager_reconcile_test.go) were left as-is since I made no changes there; only my 10 touched files were gofmt-ed
SKIP: EXTERNAL BLOCKER (not my partition): after my L2 checkpoint where the full persistence test suite ran 'ok', internal/domain/navigation/ship_navigation_calculator.go was deleted and ship.go/route.go modified by another process, removing Ship.HasCargoSpace/AvailableCargoSpace. This breaks the persistence TEST binary (persistence transitively depends on navigation via infrastructure/database), so 'go test ./internal/adapters/persistence/...' now fails to build. The persistence package itself builds and vets cleanly (go build + go vet green). Hard rule 4 forbids me from touching navigation to fix it.
SKIP: L4 parameter objects: market_repository FindCheapestMarketSelling/FindBestMarketBuying/FindFactoryForGood etc. take 4 params (ctx,goodSymbol,systemSymbol,playerID) but implement exported interface market.MarketRepository referenced outside the partition; changing signatures would break callers — out of scope.
SKIP: L4 primitive obsession: playerID passed as int/uint rather than shared.PlayerID across most methods; converting would ripple into domain interfaces outside the partition.
SKIP: L4 single-abstraction-level: modelToTask/ReconstituteTask (26 positional args) and modelToPipeline call out-of-partition domain constructors that cannot be changed here.
SKIP: L1 further constant extraction: market_repository scoreMarketForBuying magic ranking numbers (1000/10/1 weights, supply/activity ordinals) and the systemSymbol+"-%" LIKE expression left as-is to avoid churn/risk; candidate for a later pass.
SKIP: L3: no god-file splitting or feature-envy fixes applied — package is already cohesively organized one-aggregate-per-file; storage/gas repos already had toEntities/toEntity helpers matching the introduced pattern.
SKIP: L4 parameter objects for NewShip (12 params) and ReconstructShip (16 params): call sites in internal/adapters/api are outside my partition
SKIP: L4 parameter objects for NewStorageOperation (8 params) and NewRouteSegment (7 params): external call sites
SKIP: L4 replace primitive int playerID with shared.PlayerID in Route, GoodsFactory, StorageOperation, navigation events: cross-partition API change
SKIP: Collapse FlightModeStrategy pattern (3 exported strategy types + selector for a fixed priority order) into one function: exported types possibly constructible externally
SKIP: Unify duplicate goods.UnknownGoodError and goods.ErrUnknownGood: different error messages, would change observable behavior
SKIP: Delete unused Contract.IsExpired: would strand the clock field/param of exported NewContract whose callers are outside my paths
SKIP: GoodsFactory.metadata map[string]interface{} primitive obsession: needs typed metadata shared with container/persistence layers
SKIP: Full-repo go build/test currently red from a parallel agent's in-flight edits in internal/domain/manufacturing (outside my partition); my 5 packages + all compilable consumers verified green
SKIP: Dedup of the two near-identical factory vs storage COLLECTION-pipeline creation loops in pipeline_lifecycle_manager.scanForCollectionOpportunities - log message formats differ subtly per branch, behavior-exact extraction too brittle without tests (L5-L6 candidate)
SKIP: shortID helper for the pervasive id[:8] truncation pattern (40+ sites across 10 files) - mechanical but touches many fmt strings; left for a later pass
SKIP: Parameter objects for new 6-8-param unexported helpers in orphaned_cargo_handler - package has almost no tests, kept plain signatures per conservative L4 rule
SKIP: Replacing purchaser.go supplyMultipliers/CalculateSupplyAwareLimit with domain SupplyLevel.PurchaseMultiplier/CalculateSupplyAwareLimit - default-value semantics for unknown supply+activity combos need golden tests first
SKIP: Dropping unused constructor params (marketRepo, getActivePipelines) from NewTaskAssignmentManager - the call site lives in internal/application/manufacturing/commands, outside my partition
SKIP: Splitting RecoverState's per-task status loop (Steps 2a-2d) into per-status handler methods - dense continue-flow with shared mutable result; too risky without tests
SKIP: Deleting the unreachable if minUnits < 1 guard in storage_acquire_deliver_executor (availableSpace >= 1 is guaranteed by earlier early-return) - left in place, noted for L5
SKIP: Parameter objects for ReconstituteTask (31 params), ReconstitutePipeline, ReconstituteFactoryState - call sites live in internal/adapters/persistence outside the partition
SKIP: Replace primitive string supply fields (FactoryState.currentSupply/previousSupply) with SupplyLevel domain type - exported getters consumed by services outside partition
SKIP: Encapsulate shipsUsed map + mutex into a thread-safe type - would change lock granularity of existing unlocked len() reads in logging paths (pre-existing benign race preserved)
SKIP: Delete the entire unused internal/application/manufacturing/config package (grep-proven unimported) - too destructive for this pass, flag for L5-L6
SKIP: Remove now-empty TaskPriorityCalculator and LiquidationPolicy shells - still constructed by services/manufacturing/manufacturing_coordinator.go outside partition
SKIP: produceNodeOnly still has 7 params and mixes abstraction levels (buy-leaf vs fabricate flows) - further decomposition deferred
SKIP: ports.go interfaces not pruned - repository contracts implemented by adapters outside partition
SKIP: market_locator.go: FindImportMarket and FindBestExportMarket are unused repo-wide but were NOT deleted - file is under a pending captain fix report freezing exported names/signatures
SKIP: construction_pipeline_planner.go: long-function extraction of StartOrResume/createTasksForMaterial deferred - pending captain fix report freezes all function signatures in the file
SKIP: collection_opportunity_finder.go: fmt.Printf debug spam left in place (deleting changes observable stdout output = behavior change); FindStorageOpportunities buyer-index loop not unified with buildFactoryAndBuyerIndices to avoid altering repo call/filter patterns
SKIP: Manual exchange-sorts in market_locator.go (2x) and task_queue.go not converted to sort.SliceStable - tie-ordering among equal candidates could change which market/task is selected
SKIP: NewSupplyMonitor 10-parameter constructor: parameter object skipped - callers live outside the partition (cmd/spacetraders-daemon, manufacturing commands)
SKIP: CollectionOpportunity.Score magic bonus weights (100/500/300/200/50) left inline - policy tuning values with no domain names to draw on; candidates for an L5 scoring-policy type
SKIP: go build ./... intermittently failed during the session due to concurrent agents editing internal/domain/manufacturing and services/manufacturing (outside my partition); final full build passes
