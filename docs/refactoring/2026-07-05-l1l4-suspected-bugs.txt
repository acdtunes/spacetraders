BUG: ship_repository.go: the removed contains() helper had godoc claiming 'case-insensitive helper' but its implementation did case-SENSITIVE byte matching. Behavior preserved via strings.Contains (also case-sensitive), so isAlreadyDockedError/isAlreadyInOrbitError still won't match differently-cased API error text — the doc was misleading, not the code.
BUG: client.go request(): leftover DEBUG fmt.Printf statements ('[DEBUG] Got 429...', 'Sleeping...', etc.) exist only in the 429 retry branch (~lines 1641-1683), not in the 503/5xx branches. Debugging output to stdout in production; left untouched to preserve output behavior.
BUG: client.go NavigateShip(): arrivalTime is hardcoded to 0 and returned as Result.ArrivalTime (dead legacy value, self-labeled 'can be removed later'); not touched.
BUG: client.go endpointToHumanName nameMap: pattern '/my/ships/*/negotiate' maps to 'Negotiate Contract', but the real endpoint path is '/my/ships/*/negotiate/contract', so normalizePath likely never hits this key — potential metrics endpoint-label mismatch. Not fixed.
BUG: internal/domain/daemon/health_monitor.go: isShipStuck() always returns false (documented stub), so DetectStuckShips() can never report a stuck ship and its inner routes/route.Status()==Completed branch has an empty body (dead) — intentional stub, left untouched
BUG: internal/domain/daemon/health_monitor.go: AttemptRecovery() is a stub that increments SuccessfulRecoveries whenever a ship is IN_TRANSIT regardless of real recovery — appears intentional placeholder, not fixed
BUG: internal/domain/ledger/transaction.go: GetCategory() and Category() are duplicate exported getters both returning t.category — possible redundant API, not changed
BUG: scout_tour.go continuousMarketScanning: godoc states 'scans the market every 60 seconds' but implementation waits 5*time.Minute and logs duration '5m' — stale/incorrect comment; left unchanged to preserve behavior and avoid comment edits
BUG: contract/types/contract_types.go: RunFleetCoordinatorCommand.ShipSymbols is documented 'Deprecated: kept for backward compatibility, no longer used' — dead exported field; left untouched because it is a serialized command type (removing risks JSON/DB compat), flagging as dead-symbol not behavioral bug
BUG: gas/commands/siphon_resources.go: duplicated step-number comments — two consecutive '// 6.' markers (lines ~95 and ~101), and balance_ship_position.go has out-of-order step-number comments (3/6/5/6); cosmetic only, no behavior impact, not fixed
BUG: ship_cargo.go Ship.ReceiveCargo/RemoveCargo: `newCargo, _ := shared.NewCargo(...)` ignores validation error; on inconsistent cargo state s.cargo silently becomes nil
BUG: ship.go Ship.UpdateFuelFromAPI: NewFuel error silently swallowed — invalid API fuel data leaves stale fuel with no signal
BUG: storage_ship.go StorageShip.ConfirmDeposit: adds cargo without any capacity check and clamps reservedSpace at 0, so a deposit confirmed without a prior ReserveSpace can breach the capacity invariant
BUG: route.go Route.FailRoute: comment claims failure always succeeds, but LifecycleStateMachine.Fail returns error from terminal states and it is discarded with `_ =`
BUG: flight_mode_selector.go fuelPermitsMode (pre-existing logic, preserved verbatim): exact-threshold match allowed only when safetyMargin < modeCost — asymmetric special case looks unintentional
BUG: fuel.go Fuel.Consume clamps negative results to 0 instead of erroring, while Ship.ConsumeFuel pre-checks — direct Consume callers silently lose the deficit
BUG: assignment_tracker.go AssignmentTracker.Untrack: never decrements tasksByType counts (only the never-called UntrackWithType did), so GetAllocations per-type counts grow monotonically until restart, skewing WorkerReservationPolicy decisions
BUG: market_condition_checker.go MarketConditionChecker.IsFactorySupplyFavorable (dead, removed): implemented the documented stricter require-ABUNDANT-at-assignment rule, but TaskAssignmentManager preflight actually uses IsFactoryOutputReady (HIGH or ABUNDANT) - the stricter check was never wired in
BUG: task_assignment_manager.go NewTaskAssignmentManager: marketRepo and getActivePipelines params are stored in fields that are never read - dead wiring that call sites still supply
BUG: purchaser.go supplyMultipliers: duplicates the value table of domain purchaseMultipliers in internal/domain/manufacturing/supply_level.go - divergence risk if either is tuned independently
BUG: pipeline_lifecycle_manager.go scanForCollectionOpportunities: on task-persist failure the storage-opportunity path deletes the orphaned pipeline without the INFO success log that the factory-opportunity path emits - inconsistent cleanup logging
BUG: pipeline_lifecycle_manager.go AddActivePipeline(id, pipeline): id param is ignored; registry keys by pipeline.ID(), so a caller passing a mismatched id silently gets different behavior than the signature implies
BUG: run_parallel_manufacturing_coordinator.go handleWorkerCompletion: final AssignTasks call omits CoordinatorID while every other call passes cmd.ContainerID - likely an oversight; preserved as-is (kept as explicit struct literal, not converted to assignParams helper)
BUG: run_factory_coordinator.go deliverCargo/SellCargoCommand: the operation context for transaction linking was accepted but never forwarded to the sell command (params were dead and removed); factory delivery sales may not be ledger-linked to the factory operation
BUG: run_factory_coordinator.go produceNodeOnly (fabrication branch): comment admits inputs are re-produced inline instead of transferring cargo from already-completed child workers - potential double purchasing of inputs in parallel mode
BUG: internal/application/manufacturing/config package is imported by nothing in the entire repo - whole package is dead code (only redundant accessors removed; core kept)
BUG: internal/domain/manufacturing/pipeline.go Start(): tasksReady counter incremented without recalculateTaskStats, and recalculateTaskStats counts FAILED only when !CanRetry - counters can drift from actual task states (pre-existing, untouched)
BUG: market_locator.go FindBestExportMarket: after GetMarketData only err is checked, not marketData == nil - an unscanned waypoint returns (nil, nil) and marketData.FindGood(good) will nil-panic; sibling finders all check both
BUG: supply_monitor_acquisition.go createMarketAcquireDeliverTask (pre-existing in createAcquireDeliverTasksForFactory): FindExportMarketWithGoodSupply returns (nil, nil) when no HIGH/ABUNDANT market exists, but only err is checked before dereferencing exportMarket.WaypointSymbol - nil-panic path for raw materials with no well-supplied source
BUG: construction_pipeline_planner.go createProductionTasksForInput: original condition `goods.IsRawMaterial(input) || (supplyChainDepth >= 1 && goods.IsRawMaterial(input))` had a tautological second clause, suggesting the author intended different depth-1 handling for non-raw inputs; behavior preserved exactly (simplified to the equivalent boolean only)
BUG: collection_opportunity_finder.go FindOpportunities/FindStorageOpportunities: verbose fmt.Printf debug output to stdout in production code paths (kept - removing would change observable output)
